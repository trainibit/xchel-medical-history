spring:
  application:
    name: medical-history
  datasource:
    url: jdbc:postgresql://localhost:5432/xchel_medical_records
    username: postgres
    password: trainibit
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379